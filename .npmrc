# pnpm + electron-builder 打包兼容配置
# electron-builder 无法可靠解析 pnpm 默认的 symlink+虚拟 store 结构，
# 会导致部分深层依赖（如 epub-gen-memory 的 ejs）漏打包进 app.asar，
# 运行时报 "Cannot find module 'ejs'" 并使主进程崩溃、软件无法启动。
# 改用 hoisted 链接器生成扁平的 node_modules（与 npm 一致），
# 确保 electron-builder 能完整收集所有依赖。
node-linker=hoisted
